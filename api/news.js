const BASE='https://www.gov.br/cvm/pt-br/assuntos/noticias/2026';
const clean=s=>s.replace(/<[^>]+>/g,' ').replace(/&nbsp;/g,' ').replace(/&amp;/g,'&').replace(/&#\d+;/g,' ').replace(/\s+/g,' ').trim();
export default async function handler(req,res){try{const r=await fetch(BASE,{headers:{'User-Agent':'RadarFIDC/1.0'}});if(!r.ok)throw new Error('Notícias CVM indisponíveis');const h=await r.text();const re=/<a[^>]+href="([^"]+)"[^>]*>([\s\S]*?)<\/a>/gi;let m,out=[],seen=new Set();while((m=re.exec(h))){const title=clean(m[2]);let url=m[1];if(!/oferta|fidc|fundo|sre|resolu|distribui|crédito/i.test(title)||title.length<20)continue;if(url.startsWith('/'))url='https://www.gov.br'+url;if(!/^https:\/\/www\.gov\.br\/cvm/.test(url)||seen.has(url))continue;seen.add(url);out.push({title,url,source:'CVM'});if(out.length>=12)break}res.setHeader('Cache-Control','s-maxage=10800, stale-while-revalidate=43200');res.status(200).json({source:'CVM',items:out,updatedAt:new Date().toISOString()})}catch(e){res.status(502).json({error:e.message})}}

const BASE='https://www.gov.br/cvm/pt-br/assuntos/noticias/2026';
const clean=s=>s.replace(/<[^>]+>/g,' ').replace(/&nbsp;/g,' ').replace(/&amp;/g,'&').replace(/&#\d+;/g,' ').replace(/\s+/g,' ').trim();
const normalize=s=>clean(s).normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase();
export const isRelevantTitle=title=>{const t=normalize(title);if(t.length<20)return false;const strong=[/\bfidc\b/,/direitos credit/,/securitiz/,/oferta(s)? publica(s)?/,/resolucao( cvm)? 160/,/\bsre\b/,/coordenador(a)? de oferta/,/distribuicao de (valores mobiliarios|ofertas)/,/credito estruturado/];return strong.some(rx=>rx.test(t))};
export default async function handler(req,res){try{const r=await fetch(BASE,{headers:{'User-Agent':'RadarFIDC/1.1'}});if(!r.ok)throw new Error('Notícias CVM indisponíveis');const h=await r.text();const re=/<a[^>]+href="([^"]+)"[^>]*>([\s\S]*?)<\/a>/gi;let m,out=[],seen=new Set();while((m=re.exec(h))){const title=clean(m[2]);let url=m[1];if(!isRelevantTitle(title))continue;if(url.startsWith('/'))url='https://www.gov.br'+url;if(!/^https:\/\/www\.gov\.br\/cvm/.test(url)||seen.has(url))continue;seen.add(url);out.push({title,url,source:'CVM'});if(out.length>=12)break}res.setHeader('Cache-Control','s-maxage=10800, stale-while-revalidate=43200');res.status(200).json({source:'CVM',items:out,updatedAt:new Date().toISOString()})}catch(e){res.status(502).json({error:e.message,items:[]})}}

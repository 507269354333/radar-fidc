export default async function handler(req,res){res.setHeader('Cache-Control','s-maxage=300');res.status(200).json({source:'Radar FIDC',quotes:[],note:'Cotações externas não são publicadas: o painel utiliza somente fontes oficiais nos indicadores.'})}

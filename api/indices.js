import {fetchCvmMarketsDataset} from '../lib/cvm-data.js';import {fetchBcbDataset} from '../lib/bcb-data.js';import {computeRadarIndices} from '../lib/indices.js';import {previousFullMonth} from '../lib/monthly.js';
export default async function handler(req,res){try{const month=String(req.query?.month||previousFullMonth(new Date()));const [cvm,bcb]=await Promise.all([fetchCvmMarketsDataset(),fetchBcbDataset().catch(()=>null)]);const data=computeRadarIndices(cvm,bcb,month);res.setHeader('Cache-Control','s-maxage=21600, stale-while-revalidate=86400');res.status(200).json(data)}catch(e){res.status(502).json({error:e.message})}}